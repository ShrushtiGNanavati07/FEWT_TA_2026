Lab 5 Text File:
Program 1 :
Find the maximum number 
Logic => 
step1. Store the first element as maximum element(int the variable named max)
step2. Run the loop for all the elements in array 
step3. Check if it is greater than maximum element. then store it in max.

Program 2:
Sort an array in ascending order.
Logic => 
step1. Scan one element(in i th loop ) and compare it with every other element(j th loop) in the array. 
step2. Swap the position of element.
5,4,9,1
//i=0,j=1 ---> 4,5,9,1
//i=0 j=2 ---> 4,5,9,1
//i=0 j=3 ---> 1,5,9,4

//i=1 j=2 ---> 1,5,9,4
//i=1 j=3 ---> 1,4,9,5


//i=2 j=3 ---> 1,4,5,9

Program 3:
read arry from user and and sort them using bubble sort.

Logic=>
Two adjacent elements of array should be compared.
Inner Loop: Iterates through the array to compare elements at index j and j + 1.
 If arr[j] > arr[j + 1], they swap positions.

 //5 3 8 1
        //Pass 1
        //5 3 1 8

        //Pass 2
        //3 1 5 8

        //Pass 3
        //1 3 5 8

Greatest element is sorted first.

Program 4:
<!--take array from user and sort them using quick sort.-->


QuickSort is a sorting algorithm based on the Divide and Conquer that picks an element as a 
pivot and partitions the given array around the picked pivot by placing the pivot in its
correct position in the sorted array. .

Program 5:
WAP that uses loop that searches a word in sentence held in an array and returning the
index of the word

Logic =>
Take the input as a string and store it in variable.
use string.split() method. This method splits the string into array.
For example: 
sentence = "Hello World. Program 5"
arr = sentence.split(" ") //since we want to split the sentence by space we write .split(" ")
// arr is ["Hello", "World.", "Program", "5"]
then to search the index use loop or one of the string methods( indexOf() )

